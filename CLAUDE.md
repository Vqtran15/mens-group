@AGENTS.md

## CRITICAL — Project Identity

This is the **mens-group PWA** project. It is **NOT** the Covey Space app.

- `app.coveyspace.com` is the **meal-rotation** project at `/Users/vuongtran/meal-rotation/`
- This project (`mens-group-pwa`) is a completely separate app with its own Supabase instance (`ycjgfmdaecufftqzfmqj`), its own codebase (Next.js), and its own deployment
- When the user says "covey space app" or mentions `app.coveyspace.com`, they mean **meal-rotation**, not this project
- Never add features intended for meal-rotation into this repo
